(()=>{let day='A';const days=document.querySelector('#days'),muscle=document.querySelector('#muscle'),code=document.querySelector('#code'),list=document.querySelector('#exerciseList');function render(){days.innerHTML=Object.keys(WORKOUTS).map(k=>`<button class="${k===day?'active':''}" data-day="${k}">${k}</button>`).join('');const w=WORKOUTS[day];muscle.textContent=w.muscle;code.textContent=`TREINO ${day}`;list.innerHTML=w.ex.map((name,i)=>`<article class="exercise"><div class="thumb">${i+1}</div><div><strong>${name}</strong><small>4 séries</small></div><div class="arrow">›</div></article>`).join('');days.querySelectorAll('button').forEach(b=>b.onclick=()=>{day=b.dataset.day;render()})}render();if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js');})();